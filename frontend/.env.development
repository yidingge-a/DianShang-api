# 走 Vite 代理 → http://127.0.0.1:8000（见 vite.config.js server.proxy）
VITE_API_URL=/api/v1
# 本地直连后端联调时关闭 NoCode 注入脚本，避免空白页
VITE_DISABLE_NOCODE=1
# 测试阶段关闭登录关口（路由守卫 + 401 跳转）；上线前改回 0 或删除
VITE_DISABLE_AUTH=0
